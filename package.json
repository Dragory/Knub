{
  "name": "knub",
  "version": "29.0.1",
  "description": "A bot framework for Discord",
  "author": "Miikka Virtanen <contact@mivir.fi>",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "mocha --exit -r ts-node/register src/**/*.test.ts -- --trace-warnings",
    "build": "shx touch dist && shx rm -r dist && tsc --sourceMap false",
    "preversion": "npm run test",
    "prepublishOnly": "npm run test && npm run build",
    "watch": "shx touch dist && shx rm -r dist && tsc --watch",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.ts": [
      "eslint",
      "prettier --write",
      "git add"
    ]
  },
  "dependencies": {
    "eris": "^0.11.2",
    "escape-string-regexp": "^2.0.0",
    "knub-command-manager": "^7.1.0",
    "lodash.clonedeep": "^4.5.0",
    "reflect-metadata": "^0.1.13",
    "ts-essentials": "^2.0.12"
  },
  "devDependencies": {
    "@types/chai": "^4.2.3",
    "@types/mocha": "^7.0.2",
    "@types/node": "^12.12.36",
    "@typescript-eslint/eslint-plugin": "^2.28.0",
    "@typescript-eslint/parser": "^2.28.0",
    "chai": "^4.2.0",
    "eslint": "^6.8.0",
    "husky": "^3.0.5",
    "lint-staged": "^9.3.0",
    "mocha": "^7.1.1",
    "prettier": "^1.19.1",
    "shx": "^0.3.2",
    "ts-node": "^8.8.2",
    "typescript": "^3.7.5"
  },
  "peerDependencies": {
    "eris": "^0.11.0"
  },
  "files": [
    "/dist/**/!(*.test.js)"
  ]
}
